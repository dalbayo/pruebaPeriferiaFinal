# Copiar este archivo a ".env" (que debe quedar en .gitignore) y ajustar
# los valores. docker-compose lee automáticamente el archivo ".env" ubicado
# junto al docker-compose.yml.

# --- PostgreSQL ---
DB_USER=postgres
DB_PASSWORD=root
DB_EXPOSED_PORT=5432

# --- Backend ---
BACKEND_EXPOSED_PORT=8080
JWT_SECRET=EstaEsUnaClaveSuperSecretaParaPeriferia2026
JWT_EXPIRATION=3600000
